{
  "name": "s32",
  "version": "0.1.4",
  "description": "S32 Design Studio 3.5.x live introspection bridge. Gives Claude/Codex accurate menu paths, command IDs, workbench state, build/problem info, launch config analysis, and dialog inspection — so the AI stops directing users to non-existent menus or unsafe flash/debug actions.",
  "author": {
    "name": "Open-source contributors"
  },
  "homepage": "https://github.com/bigbangten/s32ds-mcp-bridge",
  "license": "MIT",
  "keywords": ["s32ds", "nxp", "eclipse", "mcp", "embedded", "cdt", "pemicro", "arm"],
  "mcpServers": {
    "s32ds": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/bootstrap_and_run.py"],
      "env": {
        "S32DS_BRIDGE_URL": "http://127.0.0.1:39231"
      }
    }
  }
}
